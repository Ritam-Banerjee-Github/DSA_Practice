1)str.substring(int start,int end)
Parameters
startIndex : starting index is inclusive

endIndex : ending index is exclusive

Returns
specified string


2)str.indexOf()
No.	Method	Description
1	int indexOf(int ch)	It returns the index position for the given char value
2	int indexOf(int ch, int fromIndex)	It returns the index position for the given char value and from index
3	int indexOf(String substring)	It returns the index(first matching index in substring) position for the given substring
4	int indexOf(String substring, int fromIndex)	It returns the start index position for the given substring searching from given index 


To convert from integer(it) to character and append in string-

 String ans = "";
        for (int it : topo) {
            ans = ans + (char)(it + (int)('a'));
        }


3) String replaced=str.replace("old","new"); // Replace one
                   = str.replaceAll("old","new"); // Replaces All