Optimal Approach : Difference Array Technique 
(Difference Array + Cummulative Sum Array to find out number of operations required for each index)

class Solution {
    public boolean isZeroArray(int[] nums, int[][] queries) {
     int n=nums.length;
     int[] difference=new int[n];

     for(int[] query:queries){
        int start=query[0];
        int end=query[1];
    
        difference[start]+=1;
        if(end+1<n) difference[end+1]-=1;
     }   

     int[] resultant=new int[n];
     resultant[0]=difference[0];

     for(int i=1;i<n;i++){
        resultant[i]=resultant[i-1]+difference[i];
     }

     for(int i=0;i<n;i++){
        if(resultant[i]<nums[i]) return false;
     }

     return true;
     
    }
}
