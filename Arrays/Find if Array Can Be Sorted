Bubble Sort - O(N^2)
class Solution {
    public boolean canSortArray(int[] nums) {
        int n=nums.length;
        for(int i=0;i<n;i++){
            boolean isSwap=false;
            //Bubble Sort : Iterate (n-1) times and compare adjacent elements. Each swap will hav max element at the end(rightmost). 
            for(int j=0;j<n-i-1;j++){
                if(nums[j]>nums[j+1]){
                    if(Integer.bitCount(nums[j])==Integer.bitCount(nums[j+1])){
                          int temp=nums[j];
                          nums[j]=nums[j+1];
                          nums[j+1]=temp;
                          isSwap=true;
                    }else{
                        return false;
                    }
                }
            }
            if(isSwap==false) break;
        }
        
        return true;
    }
}
